function addNumbers(num1, num2) {
    const total = Number(num1) + Number(num2);
    return total;
}

const addedTotal = addNumbers(1, 9);
const addedTotal2 = addNumbers(15234, 95684);

function greeting(name) {
    return `Hello ${name}`;
}

const dog = greeting("Alexa");

function timesNumbers(num1, num2) {
    const total = Number(num1) * Number(num2);
    return total;
}
const course = "JavaScript 101: Coding for beginners";
typeof(course);

let arr = [];
typeof(arr); //This will show you that the type is object, but it is actually an array

const boolean = false;
typeof(boolean);

const dog = {
    'name': 'Alexa', 
    'breed': 'Miniature Doberman Pincher',
    'age': 2
}
typeof(dog);

const num = 10;
typeof(num);